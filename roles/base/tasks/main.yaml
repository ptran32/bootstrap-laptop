- include: homebrew.yaml
  when: ansible_os_family == 'Darwin'
  sudo: yes
  
- include: apt.yaml
  when: ansible_pkg_mgr == 'apt'
  sudo: yes

- include: base.yaml
  sudo: yes

- set_fact:
    role_basic_done: true
